<Page x:Class="Magmify.Pages.Zoom"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
	xmlns:viewModels="clr-namespace:Magmify.ViewModels"
	mc:Ignorable="d"
	Loaded="Zoom_OnLoaded"
	d:DesignHeight="450" d:DesignWidth="800" FontFamily="Cascadia Mono"
	Title="{DynamicResource Page.Zoom.Title}" Background="{DynamicResource Window.Background.Secondary}"
	Foreground="{DynamicResource Text.Foreground}">
	<Page.DataContext>
		<viewModels:ZoomViewModel/>
	</Page.DataContext>
	<!-- Main Panel -->
	<StackPanel Orientation="Vertical" Margin="15 5 0 0">

		<!-- Page Header -->
		<StackPanel Orientation="Vertical">
			<ui:TextBlock FontSize="20" FontWeight="Bold" Text="{DynamicResource Label.ZoomConfiguration}"/>
			<ui:TextBlock FontSize="15" Margin="0 5 0 10" Foreground="{DynamicResource Text.Secondary.Foreground}"
						Text="{DynamicResource Label.ConfigureZoom}"/>
			<Border BorderBrush="{DynamicResource StatusBar.Border}" BorderThickness="0 2 0 0"/>
		</StackPanel>

		<!-- Page Content -->
		<StackPanel Orientation="Horizontal" Margin="0 10 0 0">

			<!-- Left Content -->
			<StackPanel Orientation="Vertical">

				<!-- Hotkeys Content -->
				<ui:TextBlock FontSize="15" Foreground="{DynamicResource Text.Foreground}" FontWeight="Bold"
							Text="{DynamicResource Label.Hotkeys}"/>
				<StackPanel Orientation="Horizontal" Margin="0 10 0 0">

					<StackPanel Orientation="Vertical">
						<ui:TextBlock FontWeight="Thin" Margin="0 0 0 5" MinWidth="100" MaxWidth="150"
									Text="{DynamicResource Label.Keybinding}" HorizontalAlignment="Left"/>
						<ui:TextBox PlaceholderText="{DynamicResource Label.NotSet}" IsReadOnly="True"
									PreviewKeyDown="Keybinding_OnPreviewKeyDown"
									Text="{Binding ZoomKeybinding, Mode=OneWay}"/>
						<StackPanel Orientation="Horizontal">
							<ui:Button x:Name="SaveButton" Margin="0 10 10 0" Click="SaveButton_Click"
										Content="{DynamicResource Button.Save}"/>
							<Label x:Name="StatusLabel" Margin="0 0 0 3" VerticalAlignment="Bottom" Visibility="Hidden"
									Foreground="{DynamicResource Text.Tertiary.Foreground}" Content="{DynamicResource Label.Unsaved}"
									MaxWidth="200"/>
						</StackPanel>
					</StackPanel>

					<StackPanel Orientation="Vertical" Margin="20 0 0 0">
						<ui:TextBlock FontWeight="Thin" Margin="0 0 0 5" Text="{DynamicResource Label.KeyMode}"/>
						<ComboBox SelectedIndex="{Binding KeyMode}">
							<ComboBoxItem Content="{DynamicResource ComboBoxItem.Hold}"/>
							<ComboBoxItem Content="{DynamicResource ComboBoxItem.Toggle}"/>
						</ComboBox>
					</StackPanel>

				</StackPanel>

				<!-- Customisation Content -->
				<ui:TextBlock FontSize="15" Foreground="{DynamicResource Text.Foreground}" FontWeight="Bold"
							Margin="0 20 0 0" Text="{DynamicResource Label.Customisation}"/>
				<StackPanel Orientation="Vertical" Margin="0 10 0 0">
					<ui:TextBlock FontWeight="Thin" Margin="0 0 0 3" Text="{DynamicResource Label.ZoomMultiplier}"/>

					<StackPanel Orientation="Horizontal">
						<Slider Name="ZoomMultiplierSlider" Value="{Binding ZoomMultiplier}" Margin="0 -3 10 0"
								Maximum="10" Minimum="0.5" IsSnapToTickEnabled="True" TickFrequency="0.5" Width="250"/>
						<ui:TextBlock Text="{Binding ElementName=ZoomMultiplierSlider, Path=Value}"
									VerticalAlignment="Center"/>
						<ui:TextBlock Text="x" VerticalAlignment="Center"/>
					</StackPanel>

					<ui:TextBlock FontWeight="Thin" Margin="0 5 0 3" Text="{DynamicResource Label.ZoomSensitivity}"/>

					<StackPanel Orientation="Horizontal">
						<Slider Name="ZoomSensitivitySlider" Value="{Binding ZoomSensitivity}" Margin="0 -3 10 0" Maximum="100"
								Minimum="0"
								IsSnapToTickEnabled="True" TickFrequency="1" Width="250"/>
						<ui:TextBlock Text="{Binding ElementName=ZoomSensitivitySlider, Path=Value}"
									VerticalAlignment="Center"/>
					</StackPanel>


				</StackPanel>

			</StackPanel>

			<!-- Right Content -->
			<StackPanel Orientation="Vertical" Margin="30 0 0 0">

				<!-- Customisation Content -->

				<ui:TextBlock FontWeight="Thin" Margin="0 0 0 5" Text="{DynamicResource Label.ScrollZoom}"/>
				<ui:ToggleSwitch OnContent="{DynamicResource ToggleSwitch.On.Enabled}"
								OffContent="{DynamicResource ToggleSwitch.Off.Disabled}" IsChecked="{Binding ScrollZoom}"/>

				<ui:TextBlock FontWeight="Thin" Margin="0 15 0 5" Text="{DynamicResource Label.HandVisibility}"/>
				<ui:ToggleSwitch OnContent="{DynamicResource ToggleSwitch.On.Visible}"
								OffContent="{DynamicResource ToggleSwitch.Off.Hidden}" IsChecked="{Binding HandVisibility}"/>

				<ui:TextBlock FontWeight="Thin" Margin="0 15 0 5" Text="{DynamicResource Label.HudVisibility}"/>
				<ui:ToggleSwitch OnContent="{DynamicResource ToggleSwitch.On.Visible}"
								OffContent="{DynamicResource ToggleSwitch.Off.Hidden}" IsChecked="{Binding HudVisibility}"/>


				<ui:TextBlock FontWeight="Thin" Margin="0 15 0 5" Text="{DynamicResource Label.ZoomAnimation}"/>
				<ComboBox SelectedIndex="{Binding ZoomAnimation}">
					<ComboBoxItem Content="{DynamicResource ComboBoxItem.None}"/>
					<ComboBoxItem Content="{DynamicResource ComboBoxItem.EaseInOut}"/>
					<ComboBoxItem Content="{DynamicResource ComboBoxItem.Elastic}"/>
				</ComboBox>

			</StackPanel>

		</StackPanel>

	</StackPanel>
</Page>